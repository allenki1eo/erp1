import { ModulePlaceholder } from "@/components/module-placeholder";
export default function Page() {
  return <ModulePlaceholder title="Recipes / BOMs" description="Beer recipes (grain bills, hops) and spirit mash bills with stages." todo={["Recipe versioning", "Stage-based ingredient lists", "Yield calculator"]} />;
}

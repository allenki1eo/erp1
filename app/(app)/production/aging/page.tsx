import { ModulePlaceholder } from "@/components/module-placeholder";
export default function Page() {
  return <ModulePlaceholder title="Aging / Barrels" description="Barrel register and maturation lots." todo={["Barrel CRUD & fills count", "Fill / empty barrel", "Angel's share calc"]} />;
}
